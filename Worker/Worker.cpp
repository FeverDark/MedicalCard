#include "pch.h"
#include "framework.h"
#include "Worker.h"
/*
DB::DB() {
	int rc = sqlite3_open(dbname, &db);
	if (rc) {
		std::cerr << "Cant open db " << sqlite3_errmsg(db);
	}
	else {
		std::cerr << "Success db";
	}
}
*/